$('.checkbox-marker').on('change', function() {
  $('.checkbox-marker').not(this).prop('checked', false);
  if($(this).is(":checked")) {
    $('#answer_<%= @answer.id %>').addClass("best-answer");
  } else {
    $('#answer_<%= @answer.id %>').removeClass("best-answer");
  }
});