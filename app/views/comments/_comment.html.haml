.comment{ id: "comment_#{comment.id}"}
  - cache [comment, current_user] do
    = comment.body

  - if current_user.try(:author?, comment)
    = link_to 'Delete', comment, method: :delete, remote: true, format: :js